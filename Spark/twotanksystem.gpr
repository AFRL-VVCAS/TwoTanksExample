project Twotanksystem is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("tts.adb");

   package Builder is
      for Global_Compilation_Switches ("ada") use ("-gnata");
   end Builder;

end Twotanksystem;

